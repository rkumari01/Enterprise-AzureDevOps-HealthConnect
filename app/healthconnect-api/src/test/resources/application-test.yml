spring:

  datasource:
    url: jdbc:h2:mem:healthconnectdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: false

  liquibase:
    enabled: false

  h2:
    console:
      enabled: true